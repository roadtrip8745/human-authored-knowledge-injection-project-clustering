# External Baseline Dependency

The deterministic experiment scripts can reuse the prior Project Signature
baseline implementation when available.

Place that repository here:

`external/mail-to-report-research/`

or set the environment variable:

`PROJECT_SIGNATURE_BASELINE_ROOT`

The baseline source is not bundled in this public package because it belongs to
a separate prior manuscript/codebase.

